---
import Layout from '../../layouts/Layout.astro';
import { Sidebar } from '../../components/Sidebar';
import { ReviewAnalyzer } from '../../components/ReviewAnalyzer';
import { BarChart3 } from 'lucide-react';
---

<Layout title="Review Analyzer - GigPilot AI">
  <div class="flex min-h-screen bg-[#F8F9FA]">
    {/* Sidebar Navigation */}
    <Sidebar currentPath="/tools/review-analyzer" client:load />

    {/* Main Content Area */}
    <main class="flex-1 flex flex-col min-w-0 overflow-y-auto">
      {/* Top Navbar */}
      <header class="h-16 border-b border-slate-200 bg-white/95 backdrop-blur-md px-6 flex items-center justify-between sticky top-0 z-30 flex-shrink-0">
        <div class="flex items-center gap-2">
          <BarChart3 className="w-5 h-5 text-emerald-500" />
          <h1 class="text-sm font-bold text-slate-900">Review Analyzer</h1>
        </div>
      </header>

      {/* Main Workspace */}
      <div class="flex-1">
        <ReviewAnalyzer client:load />
      </div>
    </main>
  </div>
</Layout>
