---
import Layout from '../../layouts/Layout.astro';
import { Sidebar } from '../../components/Sidebar';
import { PricingOptimizer } from '../../components/PricingOptimizer';
import { DollarSign } from 'lucide-react';
---

<Layout title="Pricing Optimizer - GigPilot AI">
  <div class="flex min-h-screen bg-[#F8F9FA]">
    {/* Sidebar Navigation */}
    <Sidebar currentPath="/tools/pricing-optimizer" client:load />

    {/* Main Content Area */}
    <main class="flex-1 flex flex-col min-w-0 overflow-y-auto">
      {/* Top Navbar */}
      <header class="h-16 border-b border-slate-200 bg-white/95 backdrop-blur-md px-6 flex items-center justify-between sticky top-0 z-30 flex-shrink-0">
        <div class="flex items-center gap-2">
          <DollarSign className="w-5 h-5 text-emerald-500" />
          <h1 class="text-sm font-bold text-slate-900">Pricing Optimizer</h1>
        </div>
      </header>

      {/* Main Workspace */}
      <div class="flex-1">
        <PricingOptimizer client:load />
      </div>
    </main>
  </div>
</Layout>
