---
import Layout from '../../layouts/Layout.astro';
import { Sidebar } from '../../components/Sidebar';
import { ScheduledManager } from '../../components/social/ScheduledManager';
---

<Layout title="Scheduled Posts - Social Hub - GigPilot AI">
  <div class="flex min-h-screen bg-[#F8F9FA]">
    <Sidebar currentPath="/social/scheduled" client:load />
    
    <main class="flex-1 flex flex-col min-w-0 overflow-y-auto">
      <div class="p-6 space-y-6 max-w-7xl w-full mx-auto">
        <ScheduledManager client:load />
      </div>
    </main>
  </div>
</Layout>
